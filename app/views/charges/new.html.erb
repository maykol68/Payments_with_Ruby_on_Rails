<div class="col-5">
    <%= form_for @charge do |f| %>
        <div class= "fomr-group">
            <%= f.label :amount%>
            <%= f.text_field :amount, class: "form_control"%>
        </div>
        <div>
            <%= link_to "Cancel", root_path%>
            <%= f.submit "Ir a pagar",%>
        <div>
    <%end%>
</div>
